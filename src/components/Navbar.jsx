import React, { useState } from 'react'

const Navbar = () => {

  
  return (
    <nav>
      {/* <p className='time'>{ctime}</p> */}
    <button className="noti-btn" ><i class="bi bi-bell"></i></button>
    <h2 className="todo">ToDo List</h2>
    </nav>
  )
}

export default Navbar
